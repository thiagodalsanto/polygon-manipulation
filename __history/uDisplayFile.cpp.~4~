//---------------------------------------------------------------------------

#pragma hdrstop

#include "uDisplayFile.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)


void DisplayFile::desenha(TCanvas *canvas, Janela mundo,
					   Janela vp)
{
  canvas->Pen->Width   = 3;
  canvas->Pen->Color   = clRed;
  canvas->Brush->Color = clWhite;
  canvas->FillRect(Rect(0,0,500,500));

  for (int x = 0; x < poligonos.size(); x++)
  {
	poligonos[x].desenha(canvas, mundo, vp);
  }
}

void DisplayFile::toString(TListBox *lb)
{
	lb->Items->Clear();

	for (int x = 0; x < poligonos.size(); x++)
	{
		poligonos[x].
	}
}

